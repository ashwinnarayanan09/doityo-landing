export function getBlogs() {
  const pokemonList = [];
  return pokemonList;
}

export function filterBlogs(area) {
  //let filtredBlogs = getBlogs().filter((type) => === area);
  //return filtredBlogs;
}
